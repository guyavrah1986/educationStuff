#include <stdio.h>

#include "spirentApi.h"
//#include "coreMgrWrapper.h"


/*
enum SpStatus SpM2mCreateCoreMgr(INOUT SpM2mCoreMgr** pCoreMgr)
{
	if (*pCoreMgr == NULL)
	{
		printf("SpM2mCreateCoreMgr - got a NULL pointer \n");
		return SP_M2M_STATUS_GENERAL_FAUILRE;
	}

	*pCoreMgr = CreateCoreMgr();
	return SP_M2M_STATUS_SUCCESS;
}

enum SpStatus CreateClient(INOUT struct CCoreClient** p)
{
	if (*p == NULL)
	{
		printf("CreateClient - got a NULL pointer \n");
		return SP_M2M_STATUS_GENERAL_FAUILRE;
	}

	struct CCoreMgr* mgr = CCoreMgr::GetInstance();

	return SP_M2M_STATUS_SUCCESS;
}

SpStatus CreateRsrc(IN const char* rsrcUri,
					IN EnProtocolType eType,
					IN bool bInternalCreated,
					INOUT CRsrcBase*& pRsrc)
{
	return SP_M2M_SUCCESS;
}
*/
